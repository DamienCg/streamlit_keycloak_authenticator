# Streamlit Keycloak Authenticator

A lightweight library for integrating Keycloak authentication into Streamlit applications.

## Installation

Install the library using pip:

```bash
pip install streamlit_keycloak_authenticator
